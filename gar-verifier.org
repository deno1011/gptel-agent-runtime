#+TITLE: gar-verifier: post-execution verifier + auto-retry signal
#+PROPERTY: header-args:emacs-lisp :tangle gar-verifier.el :lexical t

Module of [[file:gptel-agent-runtime.org][gptel-agent-runtime]]. Provides the post-execution verifier
that runs AFTER an action completes and grades whether the result
actually achieved the plan step's intent. The verdict drives auto-
retry-with-correction in =gar-loop='s observe→reflect path.

Why this is the unlock for the rest of the learning track: without a
"did this work?" signal, every downstream learner (memory, skill
evolution, playbook auto-refinement) is blind. The verifier is the
sensor that the rest of the loop reads.

Two operating modes (mirror of =gar-skeptic='s rule-based / model-based
design):

- /rule-based/ (default): deterministic checks on the
  =action-result= shape -- non-=ok= status, empty output where output
  was expected, output beginning with =error/failed/exception/
  traceback= keywords. Narrow on purpose so it does not over-flag.
- /model-based/: synchronously calls the model with goal + plan-step
  + actual result and asks for a strict JSON verdict
  =({passed, confidence, reason, suggested_correction})=. Uses the
  same =gptel-request= + =accept-process-output= + =gptel-abort=
  deadline pattern as =gar-skeptic-model=. Falls back to rule-based
  on timeout / abort / error / unparseable output.

The verdict drives =gar-loop=:

- =:passed t=    -> proceed to =--reflect= as before
- =:passed nil= and =attempts < max-retries+1= -> requeue the SAME
  step with =:suggested-correction= appended to its =rationale=, so
  the next =--act= incarnation has the verifier's diagnosis as
  context. Skip =--reflect= for this iteration.
- =:passed nil= and retries exhausted -> emit
  =verifier-gave-up= event, fall through to =--reflect=
  (which typically resolves the step as =failed=).

See also:
- [[file:gptel-agent-runtime.org][gptel-agent-runtime]] (master entrypoint that requires this module)
- [[file:gar-skeptic.org][gar-skeptic]] / [[file:gar-skeptic-model.org][gar-skeptic-model]] (mirror design; skeptic is
  pre-execution adversarial, verifier is post-execution graded)
- [[file:gar-loop.org][gar-loop]] (=--observe-result= calls
  =gptel-agent-runtime-verify-action-result= and routes by verdict)

<!-- gar:function-reference:start -->
* Function reference

This section is generated from the Elisp docstrings in this Org file. It is Org prose, not tangled code, so it explains the functions without changing runtime behavior.

- =gptel-agent-runtime--verifier-applies-p=: Return non-nil when the verifier should fire for plan-step RISK.
- =gptel-agent-runtime--verifier-rule-based-verdict=: Return a rule-based verdict plist for STEP and RESULT.
- =gptel-agent-runtime--verifier-build-prompt=: Format the user prompt for the model-based verifier.
- =gptel-agent-runtime--verifier-parse-verdict=: Parse a JSON verifier verdict from TEXT into the verdict plist shape.
- =gptel-agent-runtime--verifier-model-based-verdict=: Synchronously ask a model to verify STEP's RESULT in TASK's context.
- =gptel-agent-runtime--verifier-extract-item-count=: Best-effort count of enumerable items in TEXT.
- =gptel-agent-runtime--verifier-find-prior-tool-result=: Return the action-result of the most recent prior `done' step in TASK's plan whose result has non-empty output, or nil.
- =gptel-agent-runtime--verifier-completeness-applies-p=: Return non-nil when the completeness check should fire for STEP.
- =gptel-agent-runtime--verifier-completeness-heuristic-verdict=: Heuristic completeness verdict comparing STEP's RESULT against PRIOR-RESULT.
- =gptel-agent-runtime--verifier-completeness-build-prompt=: Build the user prompt for the model-based completeness verdict.
- =gptel-agent-runtime--verifier-completeness-parse-verdict=: Parse TEXT as the model-based completeness verdict JSON.
- =gptel-agent-runtime--verifier-completeness-model-based-verdict=: Synchronously ask the model to compare STEP's RESULT against PRIOR-RESULT for completeness.
- =gptel-agent-runtime--verifier-completeness-verdict=: Dispatcher for the completeness check.
- =gptel-agent-runtime--verifier-primary-verdict=: Run the primary safety verifier (rule-based or model-based) for STEP.
- =gptel-agent-runtime-verify-action-result=: Return a verifier verdict for STEP's RESULT, or nil when no verifier fires.
- =gptel-agent-runtime--verifier-should-retry-p=: Return non-nil when STEP should be auto-retried given VERDICT.
- =gptel-agent-runtime--verifier-prepare-retry=: Mutate STEP for the next --act pass, embedding VERDICT's suggested correction.
<!-- gar:function-reference:end -->

* File header

#+begin_src emacs-lisp
;;; gar-verifier.el --- post-execution verifier + auto-retry signal -*- lexical-binding: t; -*-

;; Part of deno1011/gptel-agent-runtime. Added on 2026-05-28 as PR 1 of
;; the self-reflective / learning / memorising track.

;;; Commentary:

;; Runs AFTER an action completes. Produces a verdict plist:
;;
;;   (:passed BOOL :confidence NUMBER :reason STRING
;;    :suggested-correction STRING :mode SYMBOL :step STRING :tool STRING)
;;
;; The verdict is attached to a `verifier-verdict' event for
;; observability and consumed by gar-loop's `--observe-result' to drive
;; auto-retry of the failed step with the suggested correction
;; appended to its rationale.

;;; Code:

(require 'cl-lib)
(require 'subr-x)

(declare-function gptel-agent-runtime-emit-event "gptel-agent-runtime"
                  (type &rest args))
(declare-function gptel-agent-runtime--timestamp "gptel-agent-runtime" ())
(declare-function gptel-agent-runtime-plan-step-p "gptel-agent-runtime" (obj))
(declare-function gptel-agent-runtime-plan-step-title
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-plan-step-rationale
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-plan-step-suggested-tool
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-plan-step-args
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-plan-step-risk
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-action-result-status
                  "gptel-agent-runtime" (result))
(declare-function gptel-agent-runtime-action-result-output
                  "gptel-agent-runtime" (result))
(declare-function gptel-agent-runtime-action-result-error
                  "gptel-agent-runtime" (result))
(declare-function gptel-agent-runtime-action-result-tool
                  "gptel-agent-runtime" (result))
(declare-function gptel-agent-runtime-task-goal "gptel-agent-runtime" (task))
(declare-function gptel-agent-runtime-task-notes "gptel-agent-runtime" (task))
(declare-function gptel-agent-runtime-plan-steps "gptel-agent-runtime" (plan))
(declare-function gptel-agent-runtime-plan-step-id
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-plan-step-status
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-plan-step-result
                  "gptel-agent-runtime" (step))
(declare-function gptel-agent-runtime-action-result-p
                  "gptel-agent-runtime" (obj))
;; JSON parser + balanced-brace scanner are owned by gar-skeptic-model;
;; reuse them here rather than duplicating the implementation.
(declare-function gptel-agent-runtime--skeptic-extract-json-object
                  "gar-skeptic-model" (text))
(declare-function gptel-request "gptel" (&optional prompt &rest keys))
(declare-function gptel-abort "gptel" (&optional buf))

(defvar gptel-model)
(defvar gptel-use-tools)
(defvar gptel-stream)

#+end_src

* Defcustoms + state

#+begin_src emacs-lisp
(defcustom gptel-agent-runtime-verifier-mode 'rule-based
  "How the post-execution verifier produces verdicts.
`off' disables the verifier entirely; the loop runs as before.
`rule-based' is deterministic: it fails only when the action result
status is non-ok or the output begins with error/failed/exception/
traceback keywords. Cheap and safe; default.
`model-based' calls the model with goal + plan step + actual result
via `gptel-request' with `gptel-agent-runtime-verifier-budget-ms' as
deadline, then falls back to rule-based on timeout/abort/error/
unparseable output."
  :type '(choice (const :tag "Off" off)
                 (const :tag "Rule-based (deterministic)" rule-based)
                 (const :tag "Model-based" model-based))
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-budget-ms 3000
  "Maximum milliseconds the model-based verifier may spend before falling back."
  :type 'integer
  :safe #'integerp
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-model nil
  "Model symbol used for the model-based verifier, or nil to reuse `gptel-model'.
Pin to a small fast model (e.g. a local 3B-class instruct model) to keep
verifier latency below `gptel-agent-runtime-verifier-budget-ms'."
  :type '(choice (const :tag "Use current gptel-model" nil)
                 (symbol :tag "Model id symbol"))
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-max-retries 2
  "Maximum auto-retry attempts after a verifier `passed=nil' verdict.
The retry counter is the plan-step's `attempts' slot; each --act pass
bumps it. When `attempts > max-retries + 1' (initial attempt + N
retries), the verifier stops requeuing and falls through to --reflect."
  :type 'integer
  :safe #'integerp
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-trigger-risks
  '(write shell destructive)
  "Plan-step risks that trigger the verifier.
Safe / read steps are not verified -- the cost outweighs the benefit
for cheap, easily-redoable actions. Match this against `skeptic-trigger-
risks' to keep the gate semantics consistent."
  :type '(repeat symbol)
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-completeness-mode 'heuristic
  "Operating mode for the response-completeness check.

The completeness check is orthogonal to the primary verifier
(`verifier-mode'): it asks `did the final user-facing response relay
all of the prior tool output, or did the model summarise it away?',
not `did the action succeed?'.

- `off':         no completeness check.
- `heuristic':   deterministic line-count + item-count comparison
                 between the prior step's tool output and the current
                 step's response. Cheap, sometimes false-positive on
                 prose responses.
- `model-based': sends (TOOL-OUTPUT, RESPONSE) back to the model
                 with the explicit question `does the response include
                 every item from the tool output? Y/N + missing list'.
                 One extra LLM call per response step.

A failed completeness verdict produces a retry verdict with a
:suggested-correction telling the model to relay every item, which the
existing retry path picks up."
  :type '(choice (const :tag "Off" off)
                 (const :tag "Heuristic (deterministic)" heuristic)
                 (const :tag "Model-based" model-based))
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-completeness-min-items 3
  "Don't fire the completeness check unless prior tool output has at
least this many enumerable items.  Below this count the heuristic is
noisy and the model-based check rarely catches real summarisation."
  :type 'integer
  :safe #'integerp
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-completeness-min-ratio 0.5
  "Heuristic verdict fails when (response-items / prior-items) < this
ratio.  0.5 means `the response only covers half or fewer of the prior
items' triggers a retry; bump higher to be stricter."
  :type 'float
  :safe #'floatp
  :group 'gptel-agent-runtime)

(defcustom gptel-agent-runtime-verifier-completeness-trigger-tools
  '("direct_response")
  "Tools whose action results get a completeness pass.
Default is `direct_response' since that is the user-facing render
step; the response is what we are checking for faithful relay."
  :type '(repeat string)
  :group 'gptel-agent-runtime)

(defvar gptel-agent-runtime--last-verifier-verdicts nil
  "Recent verifier verdicts, newest first, for the mission-control dashboard.")

#+end_src

* Applies-p + rule-based verdict

#+begin_src emacs-lisp
(defun gptel-agent-runtime--verifier-applies-p (risk)
  "Return non-nil when the verifier should fire for plan-step RISK."
  (and (not (eq gptel-agent-runtime-verifier-mode 'off))
       (memq risk gptel-agent-runtime-verifier-trigger-risks)))

(defconst gptel-agent-runtime--verifier-error-output-pattern
  "\\`\\(error\\|failed\\|exception\\|traceback\\)"
  "Regexp matched against (downcase output) to detect failure-looking output.
Conservative on purpose: only catches outputs that BEGIN with one of
these keywords. Designed to avoid flagging normal text that happens to
mention `error' or `failed' as substring.")

(defun gptel-agent-runtime--verifier-rule-based-verdict (step result)
  "Return a rule-based verdict plist for STEP and RESULT.
The verdict fails (`:passed nil') only when STATUS is not `ok' OR the
output begins with an error keyword. All other cases pass."
  (let* ((status (gptel-agent-runtime-action-result-status result))
         (output (or (gptel-agent-runtime-action-result-output result) ""))
         (error (gptel-agent-runtime-action-result-error result))
         (tool (or (gptel-agent-runtime-action-result-tool result) "<unknown>"))
         (output-string (if (stringp output) output (format "%s" output)))
         (looks-like-error
          (string-match-p gptel-agent-runtime--verifier-error-output-pattern
                          (downcase output-string)))
         (passed (and (eq status 'ok) (not looks-like-error)))
         (reason
          (cond
           ((not (eq status 'ok))
            (format "Tool returned status=%s%s"
                    status
                    (if error (format ", error=%s" error) "")))
           (looks-like-error
            "Tool returned ok but output begins with an error keyword.")
           (t
            "Tool returned ok status; rule-based check has no concerns.")))
         (correction
          (cond
           ((not passed)
            (format
             "Previous attempt of `%s' failed: %s. Re-issue with corrected arguments."
             tool
             (cond ((not (eq status 'ok)) (or error "unknown error"))
                   (looks-like-error
                    (substring output-string 0
                               (min 200 (length output-string))))
                   (t "unspecified")))))))
    (list :passed passed
          :confidence (if passed 0.9 1.0)
          :reason reason
          :suggested-correction correction
          :mode 'rule-based
          :step (gptel-agent-runtime-plan-step-title step)
          :tool tool)))

#+end_src

* Model-based path

#+begin_src emacs-lisp
(defconst gptel-agent-runtime--verifier-system-prompt
  "You are the runtime verifier. Be strict.

Given a goal, a plan step that was just attempted, and the actual action
result (status + output + error), evaluate whether the action achieved
the step's intent. Return a single JSON object EXACTLY in this shape:

{
  \"passed\": true | false,
  \"confidence\": 0.0-1.0,
  \"reason\": \"...\",
  \"suggested_correction\": \"...\"
}

Be strict:
- If status is not `ok', passed=false.
- If the output is empty when the goal required producing content, passed=false.
- If the output contains an error/exception message even with status=ok, passed=false.
- If the output is plausible but not clearly correct, passed=true with lower confidence.

When passed=false, the suggested_correction MUST be a concrete next action
the executor can take on the next attempt (different args, different tool,
or a clarification of what is missing). Do not say `try again' without
specifics.

Return ONLY the JSON object. No prose before or after."
  "System prompt used for the model-based verifier call.")

(defun gptel-agent-runtime--verifier-build-prompt (step result task)
  "Format the user prompt for the model-based verifier."
  (format
   (concat
    "GOAL:\n%s\n\n"
    "PLAN STEP:\n"
    "  title: %s\n"
    "  rationale: %s\n"
    "  tool: %s\n"
    "  args: %S\n\n"
    "ACTION RESULT:\n"
    "  status: %s\n"
    "  tool: %s\n"
    "  output: %s\n"
    "  error: %s\n\n"
    "Return the JSON verdict now.")
   (or (and task (gptel-agent-runtime-task-goal task)) "")
   (or (gptel-agent-runtime-plan-step-title step) "")
   (or (gptel-agent-runtime-plan-step-rationale step) "")
   (or (gptel-agent-runtime-plan-step-suggested-tool step) "")
   (or (gptel-agent-runtime-plan-step-args step) '())
   (gptel-agent-runtime-action-result-status result)
   (or (gptel-agent-runtime-action-result-tool result) "<unknown>")
   (or (gptel-agent-runtime-action-result-output result) "")
   (or (gptel-agent-runtime-action-result-error result) "")))

(defun gptel-agent-runtime--verifier-parse-verdict (text step result)
  "Parse a JSON verifier verdict from TEXT into the verdict plist shape.
Returns nil when the text cannot be parsed into a valid verdict; the
caller falls back to the rule-based verdict."
  (condition-case nil
      (let* ((json-fragment
              (gptel-agent-runtime--skeptic-extract-json-object text))
             (parsed
              (and json-fragment
                   (let ((json-object-type 'plist)
                         (json-array-type 'list)
                         (json-key-type 'keyword)
                         (json-false :json-false))
                     (with-temp-buffer
                       (insert json-fragment)
                       (goto-char (point-min))
                       (json-read)))))
             (passed-raw (and parsed (plist-get parsed :passed)))
             (passed (cond ((eq passed-raw t) t)
                           ((eq passed-raw :json-false) nil)
                           ((eq passed-raw nil) nil)
                           ((stringp passed-raw)
                            (member (downcase passed-raw) '("true" "yes" "ok")))
                           (t nil)))
             (confidence-raw (and parsed (plist-get parsed :confidence)))
             (confidence (cond ((numberp confidence-raw) confidence-raw)
                               ((stringp confidence-raw)
                                (string-to-number confidence-raw))
                               (t (if passed 0.8 0.8))))
             (reason (and parsed
                          (or (plist-get parsed :reason)
                              (plist-get parsed :Reason)
                              "")))
             (correction (and parsed
                              (or (plist-get parsed :suggested_correction)
                                  (plist-get parsed :suggested-correction)
                                  (plist-get parsed :correction)
                                  ""))))
        (when parsed
          (list :passed passed
                :confidence confidence
                :reason (format "%s" reason)
                :suggested-correction (format "%s" correction)
                :mode 'model-based
                :step (gptel-agent-runtime-plan-step-title step)
                :tool (or (gptel-agent-runtime-action-result-tool result)
                          "<unknown>"))))
    (error nil)))

(defun gptel-agent-runtime--verifier-model-based-verdict (step result task)
  "Synchronously ask a model to verify STEP's RESULT in TASK's context.
Returns the verdict plist on success; falls back to the rule-based
verdict on timeout, abort, error, or unparseable model output."
  (unless (and (fboundp 'gptel-request) (boundp 'gptel-model))
    (cl-return-from gptel-agent-runtime--verifier-model-based-verdict
      (gptel-agent-runtime--verifier-rule-based-verdict step result)))
  (let* ((fallback (gptel-agent-runtime--verifier-rule-based-verdict
                    step result))
         (budget-secs (max 0.5
                           (/ (or gptel-agent-runtime-verifier-budget-ms 3000)
                              1000.0)))
         (system gptel-agent-runtime--verifier-system-prompt)
         (prompt (gptel-agent-runtime--verifier-build-prompt step result task))
         (req-buf (generate-new-buffer " *gar-verifier-request*"))
         (response nil) (done nil)
         (gptel-model (or gptel-agent-runtime-verifier-model
                          (and (boundp 'gptel-model) gptel-model)))
         (gptel-use-tools nil)
         (gptel-stream nil))
    (unwind-protect
        (condition-case _err
            (progn
              (with-current-buffer req-buf
                (gptel-request prompt
                  :buffer req-buf
                  :stream nil
                  :system system
                  :callback (lambda (r _info) (setq response r done t))))
              (let ((deadline (+ (float-time) budget-secs)))
                (while (and (not done) (< (float-time) deadline))
                  (accept-process-output nil 0.05))
                (unless done
                  (ignore-errors (gptel-abort req-buf))
                  (setq response :timeout))))
          (error (setq response :error)))
      (when (buffer-live-p req-buf)
        (kill-buffer req-buf)))
    (cond
     ((eq response :timeout) fallback)
     ((eq response :error) fallback)
     ((eq response 'abort) fallback)
     ((stringp response)
      (or (gptel-agent-runtime--verifier-parse-verdict response step result)
          fallback))
     (t fallback))))

#+end_src

* Completeness check (orthogonal to primary verifier)

The completeness check asks a different question than the primary
verifier: not /did the action succeed/, but /did the final
user-facing response relay all of the prior tool output, or did the
model summarise it away/. It fires on =direct_response= steps when
the most recent prior =done= step in the same plan produced a
tool result with a list-like shape.

Two operating modes:

- /heuristic/: counts enumerable items (regex on common list
  patterns) in both prior tool output and the current response;
  fails when the ratio is below
  =verifier-completeness-min-ratio=.
- /model-based/: same =gptel-request= + deadline pattern as the
  primary model-based verifier; asks the model to compare the two
  texts and return strict JSON.

The verdict is a normal verifier-verdict plist
=(:passed BOOL :suggested-correction STRING ...)= and is consumed
by the same retry orchestration in =--observe-result=.

#+begin_src emacs-lisp
(defun gptel-agent-runtime--verifier-extract-item-count (text)
  "Best-effort count of enumerable items in TEXT.
Looks for common list patterns (bullets, numbered, Org TODO state
brackets, bracketed state markers from `get_todos').  Returns 0
when TEXT is nil/empty.  When multiple patterns match, returns the
highest -- a text that looks list-shaped by ANY measure is
considered list-shaped at that count."
  (if (or (null text) (and (stringp text) (string-empty-p text)))
      0
    (let* ((patterns
            '("^\\[\\(?:TODO\\|NEXT\\|IN-PROGRESS\\|WAIT\\|REVIEW\\|DONE\\)\\]"
              "^[-*+] "
              "^[0-9]+\\.[[:space:]]"
              "^[0-9]+)[[:space:]]"
              "^\\*+ \\(?:TODO\\|NEXT\\|IN-PROGRESS\\|WAIT\\|REVIEW\\) "))
           (counts
            (mapcar
             (lambda (p)
               (with-temp-buffer
                 (insert (or text ""))
                 (goto-char (point-min))
                 (let ((n 0))
                   (while (re-search-forward p nil t) (cl-incf n))
                   n)))
             patterns)))
      (apply #'max 0 counts))))

(defun gptel-agent-runtime--verifier-find-prior-tool-result (step task)
  "Return the action-result of the most recent prior `done' step in TASK's
plan whose result has non-empty output, or nil.  STEP is the current
step being verified; its own result is excluded from the search."
  (let* ((plan (and task (gptel-agent-runtime-task-notes task)))
         (steps (and plan (gptel-agent-runtime-plan-steps plan)))
         (current-id (and step (gptel-agent-runtime-plan-step-id step)))
         (last-done nil))
    (cl-loop for s in (or steps '())
             ;; Stop the moment we hit the current step.
             when (and current-id
                       (equal (gptel-agent-runtime-plan-step-id s)
                              current-id))
             return last-done
             do (let ((r (gptel-agent-runtime-plan-step-result s)))
                  (when (and (eq (gptel-agent-runtime-plan-step-status s)
                                 'done)
                             r
                             (gptel-agent-runtime-action-result-p r)
                             (let ((o (gptel-agent-runtime-action-result-output
                                       r)))
                               (and (stringp o) (not (string-empty-p o)))))
                    (setq last-done r)))
             finally return last-done)))

(defun gptel-agent-runtime--verifier-completeness-applies-p (step prior-result)
  "Return non-nil when the completeness check should fire for STEP.
Requires that the completeness mode is on, the step's tool is in the
trigger list, and PRIOR-RESULT's output has enough enumerable items."
  (and (not (eq gptel-agent-runtime-verifier-completeness-mode 'off))
       (let ((tool (or (gptel-agent-runtime-plan-step-suggested-tool step)
                       "direct_response")))
         (member tool
                 gptel-agent-runtime-verifier-completeness-trigger-tools))
       prior-result
       (gptel-agent-runtime-action-result-p prior-result)
       (let* ((prior-text (gptel-agent-runtime-action-result-output
                           prior-result))
              (prior-items
               (gptel-agent-runtime--verifier-extract-item-count prior-text)))
         (>= prior-items
             (or gptel-agent-runtime-verifier-completeness-min-items 3)))))

(defun gptel-agent-runtime--verifier-completeness-heuristic-verdict
    (step result prior-result)
  "Heuristic completeness verdict comparing STEP's RESULT against
PRIOR-RESULT.  Returns a verdict plist (passed t/nil + correction)
when applicable, nil otherwise."
  (let* ((response-text
          (or (gptel-agent-runtime-action-result-output result) ""))
         (prior-text
          (or (gptel-agent-runtime-action-result-output prior-result) ""))
         (prior-items
          (gptel-agent-runtime--verifier-extract-item-count prior-text))
         (response-items
          (gptel-agent-runtime--verifier-extract-item-count response-text))
         (min-ratio
          (or gptel-agent-runtime-verifier-completeness-min-ratio 0.5))
         (ratio (if (> prior-items 0)
                    (/ (float response-items) prior-items)
                  1.0))
         (passed (>= ratio min-ratio)))
    (list :passed passed
          :confidence (if passed 0.8 0.7)
          :reason (format
                   "Prior tool output had %d enumerable items; response surfaced %d (ratio %.2f, threshold %.2f)."
                   prior-items response-items ratio min-ratio)
          :suggested-correction
          (if passed
              ""
            (format "Your previous response only included %d of %d items from the prior tool result. Re-render the response listing EVERY item verbatim as a numbered list. Do not summarise, do not pick favourites, do not add interpretive framing."
                    response-items prior-items))
          :mode 'completeness-heuristic
          :step (or (gptel-agent-runtime-plan-step-title step) "")
          :tool (or (gptel-agent-runtime-action-result-tool result) ""))))

(defconst gptel-agent-runtime--verifier-completeness-system-prompt
  "You compare a TOOL OUTPUT against a final RESPONSE and decide whether the response relays every item from the tool output. Return ONLY a single strict JSON object with keys: passed (true|false), confidence (0..1), missing (array of item strings; may be empty), reason (string). No prose, no markdown, no code fences. If the response includes every item -- even reformatted -- passed is true. If the response summarises, drops items, or only shows a subset, passed is false and `missing' lists the dropped items."
  "System prompt for the model-based completeness verdict.")

(defun gptel-agent-runtime--verifier-completeness-build-prompt
    (step result prior-result)
  "Build the user prompt for the model-based completeness verdict."
  (format
   "STEP TITLE:\n%s\n\nPRIOR TOOL OUTPUT (the items that should be relayed):\n%s\n\nRESPONSE (what was emitted to the user):\n%s\n\nReturn the JSON verdict now."
   (or (gptel-agent-runtime-plan-step-title step) "")
   (or (gptel-agent-runtime-action-result-output prior-result) "")
   (or (gptel-agent-runtime-action-result-output result) "")))

(defun gptel-agent-runtime--verifier-completeness-parse-verdict
    (text step result prior-result)
  "Parse TEXT as the model-based completeness verdict JSON.
Returns a verifier-shaped verdict plist (`:passed' + correction etc.)
on success, nil on parse failure so the dispatcher can fall back to
the heuristic verdict."
  (let ((json (gptel-agent-runtime--skeptic-extract-json-object text)))
    (when json
      (condition-case _err
          (let* ((obj (let ((json-object-type 'plist)
                            (json-array-type 'list)
                            (json-key-type 'keyword))
                        (json-read-from-string json)))
                 (passed (eq (plist-get obj :passed) t))
                 (missing (plist-get obj :missing))
                 (reason (or (plist-get obj :reason) "")))
            (list :passed passed
                  :confidence (or (plist-get obj :confidence)
                                  (if passed 0.8 0.7))
                  :reason reason
                  :suggested-correction
                  (if passed
                      ""
                    (format
                     "The previous response dropped these items from the tool output: %S. Re-render the response with EVERY item from the tool output as a numbered list. Do not summarise."
                     (or missing '())))
                  :mode 'completeness-model-based
                  :step (or (gptel-agent-runtime-plan-step-title step) "")
                  :tool (or (gptel-agent-runtime-action-result-tool result)
                            "")))
        (error nil)))))

(cl-defun gptel-agent-runtime--verifier-completeness-model-based-verdict
    (step result prior-result &optional _task)
  "Synchronously ask the model to compare STEP's RESULT against
PRIOR-RESULT for completeness.  Falls back to the heuristic verdict on
timeout / abort / error / unparseable output."
  (unless (and (fboundp 'gptel-request) (boundp 'gptel-model))
    (cl-return-from gptel-agent-runtime--verifier-completeness-model-based-verdict
      (gptel-agent-runtime--verifier-completeness-heuristic-verdict
       step result prior-result)))
  (let* ((fallback (gptel-agent-runtime--verifier-completeness-heuristic-verdict
                    step result prior-result))
         (budget-secs (max 0.5
                           (/ (or gptel-agent-runtime-verifier-budget-ms 3000)
                              1000.0)))
         (system gptel-agent-runtime--verifier-completeness-system-prompt)
         (prompt (gptel-agent-runtime--verifier-completeness-build-prompt
                  step result prior-result))
         (req-buf (generate-new-buffer " *gar-completeness-request*"))
         (response nil) (done nil)
         (gptel-model (or gptel-agent-runtime-verifier-model
                          (and (boundp 'gptel-model) gptel-model)))
         (gptel-use-tools nil)
         (gptel-stream nil))
    (unwind-protect
        (condition-case _err
            (progn
              (with-current-buffer req-buf
                (gptel-request prompt
                  :buffer req-buf
                  :stream nil
                  :system system
                  :callback (lambda (r _info) (setq response r done t))))
              (let ((deadline (+ (float-time) budget-secs)))
                (while (and (not done) (< (float-time) deadline))
                  (accept-process-output nil 0.05))
                (unless done
                  (ignore-errors (gptel-abort req-buf))
                  (setq response :timeout))))
          (error (setq response :error)))
      (when (buffer-live-p req-buf) (kill-buffer req-buf)))
    (cond
     ((eq response :timeout) fallback)
     ((eq response :error) fallback)
     ((eq response 'abort) fallback)
     ((stringp response)
      (or (gptel-agent-runtime--verifier-completeness-parse-verdict
           response step result prior-result)
          fallback))
     (t fallback))))

(defun gptel-agent-runtime--verifier-completeness-verdict
    (step result &optional task)
  "Dispatcher for the completeness check.
Returns a verifier-shaped verdict plist when the check applies and
fires, nil otherwise (so the primary verdict still drives behaviour)."
  (let ((prior-result
         (gptel-agent-runtime--verifier-find-prior-tool-result step task)))
    (when (gptel-agent-runtime--verifier-completeness-applies-p
           step prior-result)
      (pcase gptel-agent-runtime-verifier-completeness-mode
        ('heuristic
         (gptel-agent-runtime--verifier-completeness-heuristic-verdict
          step result prior-result))
        ('model-based
         (gptel-agent-runtime--verifier-completeness-model-based-verdict
          step result prior-result task))
        (_ nil)))))

#+end_src

* Dispatcher + retry orchestration helpers

#+begin_src emacs-lisp
(defun gptel-agent-runtime--verifier-primary-verdict (step result task)
  "Run the primary safety verifier (rule-based or model-based) for STEP.
Returns nil when the verifier is off or the step's risk is below the
trigger threshold."
  (when (and (gptel-agent-runtime-plan-step-p step)
             (gptel-agent-runtime--verifier-applies-p
              (or (gptel-agent-runtime-plan-step-risk step) 'safe)))
    (pcase gptel-agent-runtime-verifier-mode
      ('rule-based
       (gptel-agent-runtime--verifier-rule-based-verdict step result))
      ('model-based
       (gptel-agent-runtime--verifier-model-based-verdict
        step result task))
      (_ nil))))

(defun gptel-agent-runtime-verify-action-result (step result &optional task)
  "Return a verifier verdict for STEP's RESULT, or nil when no verifier fires.
TASK is the active runtime task.  Runs two orthogonal checks:

- The primary verifier (safety / correctness) -- fires only for risky
  steps (`verifier-trigger-risks').
- The completeness verifier -- fires on direct-response steps when
  the most recent prior `done' step in the same plan produced a
  list-like tool result.

A failing completeness verdict ALWAYS wins, so the model is forced to
re-render with the missing items even when the primary verifier would
have considered the step a success."
  (let* ((primary (gptel-agent-runtime--verifier-primary-verdict
                   step result task))
         (completeness (gptel-agent-runtime--verifier-completeness-verdict
                        step result task))
         (verdict
          (cond
           ;; Completeness explicitly failed -- override the primary.
           ((and completeness (not (plist-get completeness :passed)))
            completeness)
           ;; No completeness check fired, return whatever primary says.
           ((not completeness) primary)
           ;; Completeness passed: combine into a single passing verdict
           ;; if the primary also passed (or was absent).
           (t (or primary completeness)))))
    (when verdict
      (push (cons (gptel-agent-runtime--timestamp) verdict)
            gptel-agent-runtime--last-verifier-verdicts)
      (when (> (length gptel-agent-runtime--last-verifier-verdicts) 50)
        (setcdr (nthcdr 49 gptel-agent-runtime--last-verifier-verdicts) nil))
      (gptel-agent-runtime-emit-event
       'verifier-verdict
       :source "verifier"
       :payload verdict
       :taint 'trusted)
      verdict)))

(defun gptel-agent-runtime--verifier-should-retry-p (step verdict)
  "Return non-nil when STEP should be auto-retried given VERDICT.
The step is eligible when:
- VERDICT is non-nil and `:passed' is nil
- the step's `attempts' counter is below `max-retries + 1' (1 initial + N)
- a `:suggested-correction' is present (without one, retrying blindly
  is no better than letting --reflect resolve the step)."
  (when verdict
    (let ((passed (plist-get verdict :passed))
          (correction (plist-get verdict :suggested-correction))
          (attempts (or (gptel-agent-runtime-plan-step-attempts step) 0))
          (max-retries (or gptel-agent-runtime-verifier-max-retries 2)))
      (and (not passed)
           (stringp correction)
           (not (string-empty-p (string-trim correction)))
           (< attempts (1+ max-retries))))))

(defun gptel-agent-runtime--verifier-prepare-retry (step verdict)
  "Mutate STEP for the next --act pass, embedding VERDICT's suggested correction.
Appends the correction to the step's `rationale' so the executor model
sees the verifier's diagnosis, resets status to `pending', and bumps a
small `(VERIFIER RETRY n)' suffix into the title for observability.

Returns STEP."
  (when (and step verdict)
    (let* ((correction (or (plist-get verdict :suggested-correction) ""))
           (reason (or (plist-get verdict :reason) ""))
           (attempts (or (gptel-agent-runtime-plan-step-attempts step) 0))
           (old-rationale (or (gptel-agent-runtime-plan-step-rationale step) ""))
           (annotation
            (format "\n\n[VERIFIER REJECTED ATTEMPT %d] %s\nCORRECTION: %s"
                    attempts reason correction)))
      (setf (gptel-agent-runtime-plan-step-rationale step)
            (concat old-rationale annotation))
      (setf (gptel-agent-runtime-plan-step-status step) 'pending)
      (gptel-agent-runtime-emit-event
       'verifier-retry-queued
       :source "verifier"
       :payload (list :step (gptel-agent-runtime-plan-step-title step)
                      :attempts attempts
                      :reason reason
                      :suggested-correction correction)
       :taint 'trusted)
      step)))

#+end_src

* File footer

#+begin_src emacs-lisp
(provide 'gar-verifier)

;;; gar-verifier.el ends here
#+end_src

<!-- gar:auto-backlinks:start -->
* Required by

  (no other modules require this one yet)
<!-- gar:auto-backlinks:end -->
